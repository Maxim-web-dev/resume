// Удобные экспорты компонентов из Shadcn/ui
import { Badge } from './badge'
import { Button } from './button'
import { Calendar } from './calendar'
import {
	Card, CardContent, CardHeader, CardTitle, CardDescription,
	CardFooter,
} from './card'
import {
	Dialog,
	DialogClose,
	DialogContent,
	DialogFooter,
	DialogHeader,
	DialogTitle,
	DialogTrigger,
} from './dialog'
import {
	DropdownMenu,
	DropdownMenuContent,
	DropdownMenuGroup,
	DropdownMenuItem,
	DropdownMenuLabel,
	DropdownMenuSeparator,
	DropdownMenuShortcut,
	DropdownMenuTrigger,
} from './dropdown-menu'
import { Form } from './form'
import { Input } from './input'
import { Label } from './label'
import {
	Popover,
	PopoverContent,
	PopoverTrigger,
} from './popover'
import { Separator } from './separator'
import { Toaster } from './sonner'
import { Textarea } from './textarea'
import {
	Tooltip,
	TooltipContent,
	TooltipProvider,
	TooltipTrigger,
} from './tooltip'

export { Badge, Button, Calendar, Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter, Dialog, DialogClose, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger, DropdownMenu, DropdownMenuContent, DropdownMenuGroup, DropdownMenuItem, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuShortcut, DropdownMenuTrigger, Form, Input, Label, Popover, PopoverContent, PopoverTrigger, Separator, Textarea, Tooltip, TooltipContent, TooltipProvider, TooltipTrigger, Toaster }